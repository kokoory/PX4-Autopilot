# Core (required)
torch>=2.0.0
onnx>=1.14.0
onnxruntime>=1.15.0
numpy>=1.24.0
pyyaml>=6.0
matplotlib>=3.7.0
pyulog>=1.0.0

# LLM API (required for reward generation)
anthropic>=0.25.0

# TFLite export/validation (optional, install separately if needed)
# tensorflow includes tflite-runtime
# pip install tensorflow    # for TFLite export
# pip install tf2onnx       # for ONNX->TFLite conversion
